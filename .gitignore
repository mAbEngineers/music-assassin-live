__pycache__/
*.pyc
.venv/
venv/

# model weights are release assets, never committed
models/*.onnx

build/
dist/
*.spec

# VB-Audio's installer, if a maintainer drops one in for an internal bundled
# build (ROADMAP D4). Never redistribute it through this repository.
packaging/windows/vendor/*.exe
